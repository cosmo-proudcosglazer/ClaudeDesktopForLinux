cd $HOME/ClaudeDesktopForLinux/
echo Put your password in, so that the script can move the files to the system.
sudo mv ./claude-desktop /usr/share/
sudo mv ./claude.desktop /usr/share/applications/
echo All done! The app should be available on your app launcher.
